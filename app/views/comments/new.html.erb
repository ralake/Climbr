<div class='postimageform' id="post-comment-form">
<%= form_for [@post, @comment] do |f| %>
	<%= f.label :comments %>
	<%= f.text_field :comments, class: 'form-control'%>
	<%= f.submit 'Post comment', class: "btn btn-default", id: "comment-button" %>
<% end %>
</div>